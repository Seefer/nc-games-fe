import React from "react";

function FilterBar() {
  return (
    <nav className="filter-bar">
      <p>Sort By:</p>
      <p>Order:</p>
      <p>Filter:</p>
    </nav>
  );
}

export default FilterBar;
